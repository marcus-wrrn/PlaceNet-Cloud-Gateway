/target
# Runtime / deployment artifacts — never commit secrets or local state.
/certs
# Generated by the gateway at startup (mosquitto.conf, broker persistence, dynsec state).
/config
/data
*.db
*.db-shm
*.db-wal
.env
deploy.conf